import express from  'express';
import {} from '../services/Stage';

const router = express.Router();

router.get('/', );
router.get('/:id', );
router.post('/', );
router.put('/', );
router.delete('/', );

export  default router;