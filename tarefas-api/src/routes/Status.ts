import express from  'express';
import {} from '../services/Status';

const router = express.Router();

router.get('/', );
router.get('/', );
router.post('/', );
router.put('/', );
router.delete('/', );

export  default router;